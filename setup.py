from setuptools import setup, find_packages

setup(
    name="data_olympics",
    version="0.1.0",
    packages=find_packages(),  # incluye mipaquete, mipaquete.funcionalidades, mipaquete.data, etc.
    install_requires=[
        # tus dependencias
    ],
    # resto de metadata…
)
